// Responsive layout breakpoints (logical CSS px).
// Keep in sync with `src/styles/_responsive.scss` ($mobile-breakpoint / $tablet-breakpoint).
//
// < MOBILE_BREAKPOINT          => mobile shell (bottom tabs + mini-player)
// >= MOBILE_BREAKPOINT         => desktop Splitter layout
// TABLET_BREAKPOINT is exposed for a future intermediate tier (not a layout in v1).

export const MOBILE_BREAKPOINT = 768;
export const TABLET_BREAKPOINT = 1024;
